```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  if(quantidade < 12){
    let maca = 1.30;
    return maca*quantidade;
  } else{
    let maca = 1;
    return maca*quantidade;
  }
}
```